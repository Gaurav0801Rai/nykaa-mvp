// Placeholder screen so the tab bar route resolves in the mockup.
export function Account() {
  return (
    <div className="cat-head" style={{ padding: '40px 16px' }}>
      <h1>Account</h1>
      <p>Orders, addresses and preferences.</p>
    </div>
  )
}
